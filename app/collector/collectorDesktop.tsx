"use client";

import { CollectorProps } from "./collectorMobile";

export default function CollectorDesktop({ samples, loading }: CollectorProps) {
    return (
        <div className="min-h-dvh w-full bg-bg p-8">
            <h1 className="text-xl font-bold text-text">Collector Dashboard (Desktop View)</h1>
            {/* UI Desktop วางตรงนี้ */}
        </div>
    );
}
